// This plugin has been merged into resourceSendStorm.js (Storm Kingdom).
// It is kept here as an empty stub to avoid errors if referenced externally.
if (require('node:worker_threads').isMainThread)
    return module.exports = { hidden: true, pluginOptions: [] }
